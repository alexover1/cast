square :: (x: int) -> int {
    return x * x;
}

fibonacci :: (n: int) -> int {
    if n <= 1 return 1;
    return n + fibonacci(n-1);
}

foobar :: () {
    x := square(5);
}

check_flag :: () -> int {
    flag := true;
    if flag return 60;
    return 420;
}

_start :: () -> int {
    fibonacci(5);
    return 0;
}

N : u8 : 255;
M : int : 0;
